You are working in the CloudVerse website repo.

GOAL:
The “What CloudVerse Offers / Economic decision logic for every layer” cards are currently long essays. Rewrite them to be short, concrete, and scannable.

STRICT RULES:
- Keep existing layout, spacing, theme, icons, borders, and card components.
- Change ONLY the text content for these 6 cards (titles + descriptions).
- No “routing”, no “blocking”, no “static analysis” wording.
- Avoid vague marketing words (“aligns”, “optimizes”, “govern”, “behavior”) unless followed by a concrete action.
- Each card must be: Title + 2 short lines + 1 subtle outcome line.
- Total per card: max ~220 characters for the description (excluding title).

WHERE:
Find the section that renders the 6 cards shown under:
“What CloudVerse Offers”
“Economic decision logic for every layer”
It may be in:
- client/src/pages/Home.tsx
or a component like:
- client/src/components/home/*

UPDATE COPY (use EXACT text below)

CARD 1 (AIX)
Title:
AIX: AI Model Economics (LLMs)
Description (2 lines):
Choose the right LLM for each prompt and workload.
Match quality, latency, and token cost—so you don’t overpay at scale.
Outcome line (subtle / muted):
Outcome: Lower blended AI $/token without quality regressions.

CARD 2 (DevX)
Title:
DevX: Engineering Economics
Description (2 lines):
Bring compute cost awareness into PRs, repos, and CI/CD.
Evaluate IaC + application code changes across 14+ languages before release.
Outcome line:
Outcome: Fewer costly changes reaching production.

CARD 3 (DataX)
Title:
DataX: Data Workload Economics
Description (2 lines):
Detect expensive query and pipeline patterns while they run.
Enforce execution policies—gate, throttle, or stop runaway workloads.
Outcome line:
Outcome: Prevent warehouse waste from compounding with AI usage.

CARD 4 (Commitments Optimizer)
Title:
Commitments Optimizer: Compute Procurement Economics
Description (2 lines):
Forecast demand and plan commitments ahead of time.
Improve blended rates and reduce long-term exposure automatically (optional).
Outcome line:
Outcome: Lower effective compute rate across providers.

CARD 5 (Volatility Control)
Title:
Volatility Control: Variance & Spike Containment
Description (2 lines):
Predict and detect abnormal spend and usage early.
Contain spikes before they become a budget incident.
Outcome line:
Outcome: Reduced cost variance and fewer surprise surges.

CARD 6 (Autonomous Optimization Engine)
Title:
Autonomous Optimization Engine: Baseline Shift
Description (2 lines):
Continuously tune live workloads with ML-driven actions.
Drive a sustained downward shift in steady-state infrastructure cost.
Outcome line:
Outcome: Permanent baseline reduction (not theoretical savings).

IMPLEMENTATION DETAILS:
- Preserve typography scale, but ensure description uses the existing muted text style.
- Add the “Outcome:” line in a smaller, more muted style (e.g., text-xs / text-sm, opacity-70) and keep it to ONE line.
- If cards are defined in an array/object, update the values there.
- Do not change card heights; if any card overflows, shorten the text slightly while preserving meaning.

DELIVERABLE:
Return the exact file diffs for:
1) the card data/array updates (where the text lives)
2) any small JSX changes needed to render the “Outcome” line consistently
