Redesign the "What AIX unlocks for AI engineering teams" section.

LAYOUT:
Change to a BENTO GRID layout with 2 rows for all 5 cards.
Cards in each row fit their layout naturally.

Bento structure:
- Row 1: 3 equal-sized cards (1 column each) - each card is 33.3% width
- Row 2: 2 equal-sized cards (1 column each) - each card is 50% width (wider than row 1)

CARD STYLING:
- Dark background with thin blue border (#007CFF)
- Rounded corners (12px)
- Generous padding inside
- Title: bold white text, larger font
- Description: light gray text below title
- Visual content inside the card at the top
- Cards in same row have same height

CARDS - Keep all content exactly the same, only change layout and add visuals:

Row 1 - Three cards (33.3% width each):

Card 1: Multi-provider routing
Visual inside card: Icon + mini routing diagram showing multiple provider options
[Keep exact same title and description]

Card 2: Policy guardrails
Visual inside card: Shield icon + visual representation of guardrails
[Keep exact same title and description]

Card 3: Right-sized GPU economics
Visual inside card: GPU/chip icon + visual showing GPU allocation
[Keep exact same title and description]

Row 2 - Two cards (50% width each - wider than row 1 cards):

Card 4: Token and GPU attribution
Visual inside card: Tag/tracking icon + mini cost breakdown visualization
[Keep exact same title and description]

Card 5: Model registry
Visual inside card: Database icon + visual showing model versions
[Keep exact same title and description]

VISUAL SPECIFICATIONS:
- Each card contains a small visual/UI mockup at the top
- Icons are part of the visual, not separate
- Visuals should be simple, clean, and represent the feature
- Colors: Use CloudVerse blue (#007CFF) for highlights
- Keep visuals minimal but meaningful
- Visuals take up ~30% of card height, text takes ~70%

SPACING:
- Equal left and right margins: 60px (desktop), 40px (tablet), 16px (mobile)
- Equal gap between all cards: consistent spacing
- Row 2 cards naturally wider due to 2-column layout
- Responsive: bento grid adjusts for tablet/mobile

ANIMATIONS:
- Cards fade in and slide up on scroll
- Stagger animation so cards appear one after another

Mobile:
- Stack to single column
- Cards maintain same styling
- Visuals remain visible
- Full-width with consistent margins

Keep ALL text content exactly the same - ONLY change layout to balanced 
bento grid (3 cards row 1, 2 cards row 2) and add visuals inside each card.