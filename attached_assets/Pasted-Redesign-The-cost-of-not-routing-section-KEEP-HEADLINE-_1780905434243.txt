Redesign "The cost of not routing" section.

KEEP HEADLINE AND DESCRIPTION AS IS:
- Headline: "The cost of not routing." (stays above, unchanged)
- Description: "Every hardcoded model endpoint is spending money without making a decision. AIX makes the decision explicitly. The saving is the difference." (stays above, unchanged)

BELOW THE HEADLINE AND DESCRIPTION:

Split into 2 halves:

LEFT HALF (50% width):
- Label: "AT SCALE (MONTHLY)" (cyan text, small)
- Table showing:
  Column headers: Monthly volume | Hardcoded spend | With AIX | Monthly saving
  Rows:
  1M requests | $2,980 | $298 | $2,682
  5M requests | $14,900 | $1,490 | $13,410
  10M requests | $29,800 | $2,980 | $26,820
  50M requests | $149,000 | $14,900 | $134,100
- Below table: "Assumptions: 40-90% cost reduction applied at 89% average. Hardcoded baseline is Claude Sonnet at $0.00298/request."

RIGHT HALF (50% width):
Two cards stacked vertically:

TOP CARD - Without AIX:
- Background: Dark with thin border
- Label: "WITHOUT AIX" (cyan text)
- Content: "Claude Sonnet, 5,537ms latency, $0.00298/req"

BOTTOM CARD - With AIX:
- Background: Dark with blue glowing border
- Label: "WITH AIX" (cyan text)
- Content: "GPT-4o-mini, 3,962ms latency, $0.00010/req"
- Result: "Result: 96.8% lower cost. 28.5% faster."

HEIGHT MATCHING:
- Table height on left = Cards height on right
- Both halves align at top and bottom

STYLING:
- Left half: Table with borders and structured layout
- Right half: Two stacked cards with borders
- Equal spacing and alignment
- Dark theme throughout

SPACING:
- Equal margins on both sides: 60px (desktop), 40px (tablet), 16px (mobile)
- Gap between left and right halves: consistent
- Vertical gap between top and bottom cards: consistent
- Responsive: Stack vertically on mobile (table on top, cards below)

ANIMATIONS:
- Fade in on scroll
- Table and cards stagger animation

Mobile:
- Stack vertically: table full width on top
- Cards full width below, stacked vertically
- Same styling and content

Keep all text content exactly the same - ONLY change layout to left/right 
split with table on left and comparison cards on right.