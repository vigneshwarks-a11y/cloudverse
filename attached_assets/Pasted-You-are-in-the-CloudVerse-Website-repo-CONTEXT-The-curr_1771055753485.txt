You are in the CloudVerse Website repo.

CONTEXT:
The current modal is called something like "Efficiency Snapshot" and contains:
- a score (e.g., 65/100),
- "estimated savings potential",
- invoice/provider/spend,
- top spend chips,
- a table of line items,
- an "Insights" section generated via LLM.

This no longer matches our positioning.
We are not a FinOps invoice analyzer. We are the Economic Decision Layer for AI infrastructure.

GOAL:
Redesign this modal to reflect our new direction:
- decision-point economics
- measurable outcomes cluster (Baseline Cost Shift, Avoided Compute Demand, Blended Compute Rate, Volatility Control)
- show “Decision Points” (Model Choice, Engineering Change, Query Execution, Capacity Commitments)
- remove ALL LLM-generated “insights” language and replace with deterministic, product-like outputs.

STRICT RULES:
- Keep the existing theme, layout density, typography, colors, border radii, shadows/glows.
- Keep the modal size/spacing roughly the same (minimal layout changes).
- Do NOT add new pages.
- Do NOT use the following words anywhere: routing, blocking, gate, gating, throttle, stop.
- No “LLM”, “AI-generated”, “assistant”, “insights” sections. No vague marketing claims.
- This is a demo UI modal. Use static example numbers/text (hardcoded) to represent the new metrics and decision points.

RENAMING:
Change modal title from "Efficiency Snapshot" to:
"Economic Decision Snapshot"
Subtitle (small muted):
"Baseline, demand avoidance, effective rate, and volatility — in one view."

SECTION A — TOP HEADER KPI (replace score area)
Replace the large “65/100” score with a 4-metric grid:

- Baseline Cost Shift: "-12%"
  subtext: "steady-state spend reduced"
- Avoided Compute Demand: "18%"
  subtext: "demand prevented pre-production"
- Blended Compute Rate: "-9%"
  subtext: "effective unit cost reduced"
- Volatility Control: "-23%"
  subtext: "variance reduced across daily spend"

These should be shown as 4 compact KPI tiles (same visual style as existing cards).

SECTION B — “Decision Points Covered” (replace invoice snapshot row)
Replace provider/billing period/total spend/line items with:
A row of 4 labeled chips with small icons (existing icon set):
- Model Choice
- Engineering Change
- Query Execution
- Capacity Commitments

Under that row, add a single muted line:
"CloudVerse applies economic intent where decisions are made — not after the invoice."

SECTION C — “What changed” (replace top spend chips + line items table)
Replace “Top spend” chips + “Top line items detected” table with a 2-column layout:

Left column: “Economic Changes Detected” (list of 4 items with numeric deltas)
1) "Baseline shifted down" — "-$41K/month"
2) "Demand avoided" — "1.8M tokens + 420 vCPU-hrs"
3) "Effective rate improved" — "-$0.012 per compute unit"
4) "Spike risk contained" — "3 anomalies neutralized"

Right column: “Where it happened” (list of 4 items)
1) "Model selection decisions" — "2 cheaper equivalents adopted"
2) "Engineering changes" — "3 high-cost patterns corrected pre-release"
3) "Warehouse execution" — "2 runaway queries contained"
4) "Commitments" — "coverage increased +8% (optional automation)"

Keep wording concrete and product-like. No “recommendation”, no “insight”.

SECTION D — CTA AREA (keep buttons but rename)
Keep the bottom CTA buttons area and styling.
Rename buttons to:
Primary: "Book a demo"
Secondary: "Run another snapshot"

OPTIONAL: Add a tiny footnote line above CTAs (muted):
"Snapshot is illustrative. Full metrics are computed from connected environments."

IMPLEMENTATION TASKS:
1) Locate the modal component (search for "Efficiency Snapshot" / "estimated savings potential" / "line items parsed").
2) Update the JSX and copy per above.
3) Ensure responsive behavior matches current modal (no overflow, no cramped text).
4) Remove any LLM-driven text generation logic in this modal (if present). Use static mock data.

DELIVERABLE:
Return the updated file(s) with exact diffs.
Run npm test / npm run dev to confirm the modal renders.
