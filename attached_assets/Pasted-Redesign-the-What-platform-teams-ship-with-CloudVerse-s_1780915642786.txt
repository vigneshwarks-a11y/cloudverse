Redesign the "What platform teams ship with CloudVerse" section.

LAYOUT:
Change from 2-column layout to a 2x2 grid layout with 4 equal-sized cards.
All cards must have EQUAL HEIGHT and EQUAL WIDTH.

Grid structure:
- Row 1: 2 cards (50% width each)
- Row 2: 2 cards (50% width each)
- All 4 cards same height

CARD STYLING:
- Dark background with thin border
- Rounded corners (12px)
- Generous padding inside
- Title: bold white text at top
- Description: light gray text
- Visual/chart area: inside card below description

INTERNAL STRUCTURE - ALL CARDS MUST BE IDENTICAL:

Every card layout:

TITLE (top) - FIXED, EQUAL SIZE IN ALL CARDS
- Font size: same across all 4 cards
- Bold white text
- Padding: same in all cards

DESCRIPTION (middle) - FIXED, EQUAL SIZE IN ALL CARDS
- Font size: same across all 4 cards
- Light gray text
- Padding: same in all cards
- Takes appropriate space

VISUAL AREA (bottom) - FIXED, EQUAL SIZE IN ALL CARDS
- Chart, table, or data visualization
- Each card shows relevant visual based on its content
- Takes remaining space below description
- All visuals same height proportionally

Card 1: [Keep exact same title and description]
Visual: [Relevant visualization for this feature]

Card 2: [Keep exact same title and description]
Visual: [Relevant visualization for this feature]

Card 3: [Keep exact same title and description]
Visual: [Relevant visualization for this feature]

Card 4: [Keep exact same title and description]
Visual: [Relevant visualization for this feature]

SIZING - BALANCED AND EQUAL:
- All 4 cards: 50% width (2 columns)
- All 4 cards: same height
- No card should be taller or wider than another
- Perfect 2x2 grid alignment

SPACING:
- Equal left and right margins: 60px (desktop), 40px (tablet), 16px (mobile)
- Equal gap between all cards: consistent spacing
- Responsive: adjusts for tablet/mobile

ANIMATIONS:
- Cards fade in and slide up on scroll
- Stagger animation so cards appear one after another

Mobile:
- Stack to single column
- Cards maintain styling
- All cards same width (100%)
- Consistent height behavior

Keep ALL text content exactly the same - ONLY change to 2x2 grid layout 
and add relevant visuals based on each card's specific content.