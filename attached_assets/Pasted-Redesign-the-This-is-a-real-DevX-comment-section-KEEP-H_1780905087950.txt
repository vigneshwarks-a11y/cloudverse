Redesign the "This is a real DevX comment" section.

KEEP HEADLINE AND DESCRIPTION AS IS:
- Headline: "This is a real DevX comment." (stays above, unchanged)
- Description: "This is a real DevX output. An infrastructure change that 
  looked routine. NAT gateway flag and a compute resize." (stays above, unchanged)

BELOW THE HEADLINE AND DESCRIPTION:

Split into 2 halves with EQUAL HEIGHT:

LEFT HALF (50% width):
- Code window with Mac chrome (red, yellow, green dots)
- Code shows the infrastructure changes
- Blue glowing border around code window
- Code window stretches to match the height of the right side

RIGHT HALF (50% width):
Two cards stacked vertically with EQUAL HEIGHT to code window:

TOP CARD - DevX Cost Estimate (takes ~40-45% of right half height):
- Background: Dark with thin border
- Label: "DEVX COST ESTIMATE" (small cyan text)
- Cost amount: "+$1.1k to $1.4k" (large, bold white)
- Subtext: "per month" (light gray)
- Section: "PRIMARY DRIVERS" (cyan label)
- Bullet list:
  • NAT Gateway hourly + data processing charges
  • Over-provisioned compute for observed utilisation

BOTTOM CARD - Why This Matters (takes ~55-60% of right half height):
- Background: Dark with blue glowing border
- Label: "WHY THIS MATTERS" (cyan text)
- Bullet list:
  • Always-on NAT in non-prod is a recurring cost with no production benefit
  • Instance size exceeds observed utilisation
- Section: "SUGGESTED FIX" (cyan label)
- Bullet list:
  • Disable NAT Gateway in non-prod environments
  • Use VPC endpoints for S3 and DynamoDB access
  • Right-size compute for non-prod workloads

HEIGHT MATCHING:
- Left code window height = Right side total height (both cards combined)
- Both halves align at top and bottom
- Cards expand/shrink together to match code box height

STYLING:
- Left half: Code window with blue border
- Right half: Two stacked cards with borders
- Equal spacing and alignment
- Dark theme throughout

SPACING:
- Equal margins on both sides: 60px (desktop), 40px (tablet), 16px (mobile)
- Gap between left and right halves: consistent
- Vertical gap between top and bottom cards: consistent
- Responsive: Stack vertically on mobile (code on top, cards below)

ANIMATIONS:
- Fade in on scroll
- Code window and cards stagger animation

Mobile:
- Stack vertically: code window full width on top
- Cards full width below, stacked vertically
- Same styling and content

Keep all text content exactly the same - ONLY change layout below headline/description 
to left/right split with EQUAL HEIGHT for code box and right side cards.