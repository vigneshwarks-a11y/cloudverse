You are working in the CloudVerse website repo.

GOAL:
Update the DevX section under:
"What CloudVerse Offers – Economic Decision Logic Across the AI Stack"

DevX must clearly state:
- It analyzes BOTH application source code (14+ languages) AND Infrastructure-as-Code
- It operates in PRs, repositories, and CI/CD pipelines
- It evaluates economic impact BEFORE deployment
- It influences architectural and runtime decisions
- No use of the words: routing, block, blocking, gate, gating, throttle, stop

Maintain:
- Existing layout
- Card/grid structure
- Icons
- Spacing
- Theme
- Typography

Only update the DevX title + description copy.

====================================================
REPLACE DEVX CARD CONTENT WITH:
====================================================

Title:
DevX: Engineering Decision Economics

Description:

DevX embeds economic decision logic directly into pull requests, repositories, and CI/CD pipelines.

It analyzes both Infrastructure-as-Code (Terraform, Pulumi, CDK, ARM, and related frameworks) and application source code across 14+ supported languages.

When developers introduce changes — whether provisioning infrastructure or modifying runtime logic — DevX evaluates the projected compute and cost impact before deployment.

It identifies economically significant patterns such as oversized compute definitions, inefficient data processing logic, high-cost API and model usage, unbounded concurrency, and storage-intensive architectures.

This evaluation occurs inside the developer workflow — at PR and pipeline stages — before changes reach production.

====================================================
STRATEGIC OUTCOME (if there is a second line or small caption):
====================================================

Prevents structural cost increases from entering production by influencing engineering decisions at the point of change.

====================================================
ACCEPTANCE CRITERIA:
====================================================

- DevX clearly states it analyzes application code across 14+ languages
- PRs, repos, and pipelines are explicitly mentioned
- No mention of routing, blocking, or gating
- No FinOps-style language
- Layout and styling unchanged
- Page renders cleanly on mobile and desktop

Run:
npm run dev

Return only the modified code.
