You are working in the CloudVerse website repo. Maintain the current theme, layout, typography, components, colors, logos, and overall design system. Make minimal changes, but update copy and integrations to reflect the new positioning.

GOAL:
Reposition CloudVerse away from FinOps language and toward:
- "Enterprise AI Infrastructure Economics Platform"
- Tagline: "Making AI infrastructure decisions economically intentional."
- Emphasize: decision-time enforcement (AIX routing, DevX PR gates, DataX query policies/shutdown) and procurement automation (commitments can be executed automatically but not forced).

Also: update Integrations to explicitly show integrations for AIX, DevX, DataX under the Integrations page with a new module filter and module chips on cards.

DO NOT:
- change logos, colors, spacing, layout patterns, or major component structure
- introduce new visual design styles
- remove existing sections unless necessary
- use the word "govern" anywhere
- revert to generic FinOps phrases like "cloud financial management", "unified visibility", "allocation", "chargeback" in hero copy

====================================================================
1) HOMEPAGE COPY UPDATE (minimal changes)
====================================================================

A) Update document title on Home page:
File: client/src/pages/Home.tsx
Current:
  document.title = "CloudVerse™ — Cloud Financial Management";
Change to:
  document.title = "CloudVerse™ — AI Infrastructure Economics";

B) Replace Hero headline + subcopy in Home.tsx
Find the Hero section headline:
  "Cloud financial management for modern enterprises."
Replace with:
  "Enterprise AI Infrastructure Economics Platform"

Replace the primary hero paragraph (currently: unified visibility/allocation/anomalies/etc) with:
  "Making AI infrastructure decisions economically intentional."

Replace the next paragraph (currently: private deployment/air-gapped) with:
  "CloudVerse embeds economic decision logic across model selection, workload behavior, and compute procurement — so technical execution and financial intent stay aligned."

Keep the existing CTAs/buttons as-is.

C) Update the pillar cards (keep the same grid and PillarCard component, only change the titles/descriptions)
In Home.tsx update the pillarCards array to remove FinOps phrasing and map to the real modules and enforcement:

Use these 6 cards (keep icons as close as possible; do not add new icon imports unless needed):

1. Title: "AIX: Model & Routing Economics"
   Desc: "Select the right models at the right cost-performance point — with runtime routing that prevents accidental spend."

2. Title: "DevX: Shift-left Economic Gates"
   Desc: "Block PRs that introduce economically risky code and infrastructure patterns before they reach production."

3. Title: "DataX: Data Workload Economics"
   Desc: "Enforce policies on warehouse workloads — gate, throttle, or stop inefficient queries before they create exposure."

4. Title: "Commitments Optimizer"
   Desc: "Plan and execute commitments programmatically to secure lower effective rates based on real demand signals."

5. Title: "Optimization Engine"
   Desc: "Lower your baseline by continuously tuning live workloads using ML-driven recommendations with guardrails."

6. Title: "Volatility Control"
   Desc: "Detect and contain usage spikes early to reduce variance and prevent weekend-scale surprises."

D) Insert two new sections under the hero (before <HowItWorks />), matching your current section styling:
File: client/src/pages/Home.tsx
Insert AFTER the hero section (after the closing </section> of hero) and BEFORE the border-t wrapper that contains <HowItWorks />.

SECTION 2 (Decision Point):
Heading:
  "AI Introduces Economic Consequences Into Technical Decisions"
Body:
  "Every AI deployment, model swap, and data workload carries immediate infrastructure implications. In most organizations, these decisions are optimized for performance in isolation — leaving financial consequences to be discovered only after the invoice arrives."
Then:
  "CloudVerse ensures infrastructure decisions reflect economic intent at the moment they are made."
Then 3 short lines (stacked, each its own <p> for the dramatic rhythm, same muted styling):
  "Not after the invoice."
  "Not during the post-mortem."
  "At the decision point."

SECTION 3 (Category-defining):
Heading:
  "The Economic Decision Layer for AI Infrastructure"
Body:
  "Modern AI stacks include models, data platforms, orchestration, and infrastructure. What they lack is embedded economic decision logic."
Then:
  "CloudVerse operates as the economic decision layer — influencing model selection, blocking inefficient deployments, shaping live workload behavior, and structuring long-term capacity commitments."
Then:
  "Without this layer, infrastructure decisions optimize for speed and performance — not economic consequence."

Below that body, add a 3-column set of mini blocks (use existing grid patterns) with:
- "Model & Workload Selection" + 2 lines:
  "AIX evaluates cost-performance curves at runtime."
  "DevX blocks economically risky changes before production."
- "Active Infrastructure Behavior" + 2 lines:
  "DataX enforces execution policies on live workloads."
  "Inefficient queries can be gated, throttled, or stopped automatically."
- "Commitment & Procurement Strategy" + 2 lines:
  "Commitments can be executed programmatically."
  "Procurement becomes systematic — not reactive."

E) Add the dark minimal diagram component
Create a NEW component:
File: client/src/components/home/EconomicDecisionLayerDiagram.tsx

This component must render a clean dark minimal stack diagram consistent with existing tokens:
- Use existing background/surface classes (cv-*)
- Five stacked boxes:
  Applications
  AI Models & Inference
  Data & Pipelines
  CI/CD & Deployment
  Cloud Infrastructure
- Add a horizontal band across all boxes labeled:
  "ECONOMIC DECISION LAYER"
  "CloudVerse"
- Keep it minimal: no complex SVG, no new design language, no gradients beyond what the site already uses.

Then import and place it inside SECTION 3 after the paragraph that ends with "economic consequence." and before the 3-column influence blocks.

F) Update HowItWorks steps to match the new narrative (keep component structure, only change text)
File: client/src/components/home/HowItWorks.tsx
Update the `steps` array to:

01 title: "Integrate"
body: "Connect securely across cloud, data, and AI environments."
bullets:
  - "Read-only ingestion where needed"
  - "Tenant isolation by design"
visualLabel: "Integration"

02 title: "Decide"
body: "Embed economic decision logic across model choice, deployments, and runtime policies."
bullets:
  - "Decision thresholds & guardrails"
  - "Policy-driven enforcement points"
visualLabel: "Decision logic"

03 title: "Enforce"
body: "Prevent accidental economics with automated gates, controls, and programmatic commitments."
bullets:
  - "PR gates + runtime policies"
  - "Commitment execution (optional automation)"
visualLabel: "Enforcement"

Keep animations and layout unchanged.

====================================================================
2) INTEGRATIONS: AIX / DevX / DataX module visibility
====================================================================

A) Extend Integration type with a module tag field
File: client/src/data/integrationsData.ts
Add:
  products?: ("AIX" | "DevX" | "DataX")[];

B) Update existing integrations with products tags (minimal, sensible mapping)
- AWS / Azure / GCP / OCI / Alibaba / Huawei / Tencent: products: ["AIX","DevX","DataX"] (they underpin all)
- Databricks: ["DataX"]
- Snowflake: ["DataX"]
- OpenAI: ["AIX"]
- Kubernetes/OpenShift/vCenter: ["DevX"] (and optionally DataX if you want; keep minimal)
- OAuth Microsoft/Google + Entra: ["DevX"] (auth into dev workflows), also apply to AIX if you want but keep minimal
- Jira/ServiceNow: ["DevX"] (workflows/tickets)
- Slack/Teams: ["DevX"]
- Cloud Monitoring: ["DevX"]
- Top SaaS: leave empty or ["DevX"] (optional)

C) Add missing AIX/DevX/DataX integrations as Coming Soon (to satisfy “show all integrations”)
Add new Integration entries (status "Coming soon") using existing logo assets if available; if no logo exists, use "/logos/saas.svg" as a placeholder.

Add these entries (at least):
AIX (AI Providers):
- Anthropic (Coming soon)  category: "AI"  products:["AIX"]
- Azure OpenAI (Coming soon) category:"AI" products:["AIX"] (logo can use "/logos/azure.svg" or "/logos/azure/azure.svg" if exists; otherwise saas.svg)
- AWS Bedrock (Coming soon) category:"AI" products:["AIX"] (logo can use "/logos/aws.svg")
- Google Vertex AI (Coming soon) category:"AI" products:["AIX"] (logo can use "/logos/gcp.svg")

DevX (Dev workflow):
- GitHub (Coming soon) category:"SaaS" products:["DevX"]
- GitLab (Coming soon) category:"SaaS" products:["DevX"]
- Azure DevOps (Coming soon) category:"SaaS" products:["DevX"]
- Terraform (Coming soon) category:"Infrastructure" products:["DevX"]

DataX (Data platforms):
- BigQuery (Coming soon) category:"Data" products:["DataX"]
- Amazon Redshift (Coming soon) category:"Data" products:["DataX"]
- Azure Synapse / Fabric (Coming soon) category:"Data" products:["DataX"]

For each new entry fill:
- short: one sentence aligned with economics/decision point (no FinOps buzz)
- whatWeIngest: 3–4 bullets
- outputs: 3–4 bullets
- setup.method/timeToValue/permissions: concise placeholders

D) Show module chips on integration cards
File: client/src/components/IntegrationCard.tsx
If integration.products exists and has values, render small chips (e.g., AIX / DevX / DataX) near the name or under the short description.
Use existing styling patterns (small rounded border pills). Keep subtle.

E) Add module filter on Integrations page + module sections
File: client/src/pages/Integrations.tsx

1) Update hero copy:
H1 should remain "Integrations" page style but rewrite:
- Title: "Integrations for AI Infrastructure Economics"
- Body: "CloudVerse integrates into the AI stack — model routing (AIX), PR enforcement (DevX), and workload controls (DataX) — so economic intent is applied at the decision point."

2) Add a new filter row above Category:
MODULES = ["All", "AIX", "DevX", "DataX"]
State: selectedModule (default "All")
Update filter function:
matchesModule = selectedModule==="All" || (integration.products?.includes(selectedModule as any))
Include matchesModule in final return.

3) Add an “At a glance” module section above the explorer grid (minimal)
Just below the hero, before search input, add three small blocks with:
- AIX Integrations (count of integrations with products includes AIX)
- DevX Integrations (count)
- DataX Integrations (count)
Each block can be a Link that sets selectedModule and scrolls to explorer (use onClick to setSelectedModule and then document.getElementById("explorer")?.scrollIntoView()).

Keep design consistent with existing cards.

====================================================================
3) Acceptance Criteria
====================================================================

- Home page hero no longer says "Cloud financial management" or "Unified visibility, allocation..."
- Hero tagline is exactly: "Making AI infrastructure decisions economically intentional."
- Section 2 includes the three-line rhythm ending with "At the decision point."
- Section 3 uses the "Economic Decision Layer" framing and includes the new diagram component.
- HowItWorks steps updated to Integrate / Decide / Enforce with new bullets.
- Integrations page allows filtering by module (AIX/DevX/DataX) and shows module chips on cards.
- Integrations data includes new “Coming soon” entries for AIX providers, DevX SCM tools, and DataX warehouses.
- No new global styling framework changes; minimal diff; site builds and routes work.

After changes, run:
- npm run check
- npm run dev (ensure pages render and filters work)

Return only the code changes.
